java {
    toolchain.languageVersion.set(JavaLanguageVersion.of(17))
}

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(":data-moddel"))
    implementation(project(":business-logic"))
}

application{
    mainClassName = "com.example.MyApplication"
}